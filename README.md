# Genealogy Places api

This package is the one-to-one **api** presentation adapter for `liberusoftware/module-genealogy-places`.

It owns only api transport/presentation integration. Domain rules, persistence, authorization, tenancy, and lifecycle behavior remain in the matching core package.

- Composer package: `liberusoftware/module-genealogy-places-api`
- Installer name: `genealogy-places-api`
- Package type: `liberu-module`

